rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow users to read and write only if they are authenticated
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
